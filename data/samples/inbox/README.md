# Sample RFQ inbox

One file per incoming RFQ, for intake's demo/ingest path (`scripts/ingest_inbox.py`).
Format is free for the intake owner to define; these are plain-text emails carried
over from the POC scenarios. Customers and products reference `data/reference/`.
